import React from 'react';
import Layout from '../components/Layout/Layout';
import './styles/global.css';

const Home = () => {
	return (
		<>
			<Layout className={} />
		</>
	);
};

export default Home;
